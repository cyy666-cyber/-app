# DeepSeek Mobile App - 大学生个性化学习与组队共创App

## 项目简介

一款集成 DeepSeek AI 大模型的多功能移动应用，主要功能包括：
1. **AI学习规划功能** - AI聊天、日程计划、技能树、知识库
2. **校内论坛** - 用户发起/加入论坛，内置AI助手
3. **组队功能** - 组队邀请、队伍聊天室、AI助手
4. **个人主页和排行榜** - 学习数据统计和排名

## 技术栈

### 前端
- **框架**: React 19 + Vite 7
- **样式**: Tailwind CSS 4
- **移动端**: Capacitor iOS
- **开发工具**: ESLint

### 后端
- **运行时**: Node.js
- **框架**: Express 5
- **AI集成**: OpenAI SDK (兼容 DeepSeek API)
- **中间件**: CORS, dotenv

## 项目结构

```
cursor/
├── client/              # 前端项目
│   ├── src/             # React 源代码
│   ├── ios/             # iOS 原生项目
│   ├── dist/            # 构建输出
│   └── package.json
├── server/              # 后端项目
│   ├── index.js        # Express 服务器
│   ├── .env           # 环境变量（需要配置）
│   └── package.json
├── package.json        # 根目录脚本
└── README.md
```

## 快速开始

### 1. 环境要求

- Node.js (推荐 v18+)
- npm 或 yarn
- Xcode (用于 iOS 开发)
- macOS (用于 iOS 开发)

### 2. 安装依赖

```bash
# 安装前端依赖
cd client
npm install

# 安装后端依赖
cd ../server
npm install
```

### 3. 配置环境变量

编辑 `server/.env` 文件：

```env
DEEPSEEK_API_KEY=你的DeepSeek_API密钥
PORT=3001
```

### 4. 启动开发服务器

**前端开发服务器**（终端 1）：
```bash
npm run dev:client
# 或
cd client && npm run dev
```

**后端服务器**（终端 2）：
```bash
npm run dev:server
# 或
cd server && npm run dev
```

### 5. iOS 开发模式

**实时预览模式**（推荐）：
```bash
cd client
npm run dev:ios      # 配置开发模式并同步
npm run open:ios     # 在 Xcode 中打开
```

然后在 Xcode 中选择模拟器并运行应用。

**生产构建模式**：
```bash
cd client
npm run build:ios    # 构建并同步
npm run open:ios     # 在 Xcode 中打开
```

## 开发工作流

### 日常开发

1. **启动开发服务器**：
   ```bash
   # 终端 1: 前端
   npm run dev:client
   
   # 终端 2: 后端
   npm run dev:server
   ```

2. **iOS 实时预览**：
   ```bash
   cd client
   npm run dev:ios    # 配置开发模式
   npm run open:ios  # 打开 Xcode
   ```

3. **修改代码**：
   - 前端代码在 `client/src/` 中修改
   - 后端代码在 `server/` 中修改
   - 保存后自动刷新（实时预览模式）

### Git 工作流

```bash
# 查看状态
git status

# 添加更改
git add .

# 提交
git commit -m "描述更改"

# 推送到 GitHub
git push
```

## API 接口

### 健康检查
```
GET /api/health
```

### AI 聊天
```
POST /api/chat
Body: { "message": "你的消息" }
```

## 开发脚本

### 根目录脚本
- `npm run dev:client` - 启动前端开发服务器
- `npm run dev:server` - 启动后端服务器

### 前端脚本 (client/)
- `npm run dev` - 启动 Vite 开发服务器
- `npm run build` - 构建生产版本
- `npm run build:ios` - 构建并同步到 iOS
- `npm run sync:ios` - 同步到 iOS 项目
- `npm run open:ios` - 在 Xcode 中打开
- `npm run dev:ios` - 配置开发模式（实时预览）
- `npm run prod:ios` - 配置生产模式

### 后端脚本 (server/)
- `npm run dev` - 启动 Express 服务器

## 注意事项

1. **DeepSeek API 密钥**：需要在 `server/.env` 中配置
2. **iOS 开发**：需要 Xcode 和 macOS
3. **实时预览**：确保 Mac 和模拟器在同一网络
4. **IP 地址**：如果 IP 变化，更新 `client/capacitor.config.dev.ts`

## 开发计划

按照以下阶段进行开发：

1. ✅ **第一阶段**：项目基础架构和用户系统
2. 🔄 **第二阶段**：AI学习规划功能
3. ⏳ **第三阶段**：个人主页和排行榜
4. ⏳ **第四阶段**：校内论坛功能
5. ⏳ **第五阶段**：组队功能
6. ⏳ **第六阶段**：优化和部署

详细开发计划请参考项目文档。

## 许可证

ISC

## 联系方式

- GitHub: [cyy666-cyber/-app](https://github.com/cyy666-cyber/-app)

