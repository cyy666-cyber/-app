# 开发指南：Xcode 实时预览

## ✅ 当前状态检查

### Git 连接状态
- ✅ Git 仓库已初始化
- ✅ 已连接到 GitHub: `https://github.com/cyy666-cyber/-app.git`
- ✅ Xcode 项目文件已提交到 Git
- ✅ Xcode 可以自动识别 Git 仓库

### Xcode 项目状态
- ✅ Xcode 项目文件存在: `client/ios/App/App.xcodeproj`
- ✅ Capacitor iOS 平台已配置
- ✅ Web 资源已同步到 iOS 项目

## 🚀 两种开发模式

### 模式 1：实时预览模式（推荐开发时使用）

**特点**：代码修改后立即在模拟器中看到效果，无需重新构建

**步骤**：

1. **启动前端开发服务器**（在终端 1）：
```bash
cd client
npm run dev
```
服务器会在 `http://localhost:5173` 启动

2. **配置开发模式并同步到 iOS**（在终端 2）：
```bash
cd client
npm run dev:ios
```
这会：
- 复制开发配置到 `capacitor.config.ts`
- 同步配置到 iOS 项目
- 配置 iOS 应用连接到开发服务器

3. **在 Xcode 中打开项目**：
```bash
cd client
npm run open:ios
```

4. **在 Xcode 中运行**：
   - 选择 iOS 模拟器（如 iPhone 15 Pro）
   - 点击运行按钮（▶️）
   - 应用会连接到 `http://192.168.1.104:5173` 的开发服务器

5. **实时预览**：
   - 修改 `client/src/` 中的代码
   - 保存文件
   - Vite 会自动热更新
   - 模拟器中的应用会自动刷新显示最新内容

**注意事项**：
- 确保 Mac 和模拟器在同一网络（模拟器使用 Mac 的网络）
- 如果 IP 地址变化，需要更新 `capacitor.config.dev.ts` 中的 `url`
- 获取当前 IP：`ifconfig | grep "inet " | grep -v 127.0.0.1`

### 模式 2：生产构建模式（测试最终效果）

**特点**：构建优化后的代码，测试最终用户体验

**步骤**：

1. **构建并同步到 iOS**：
```bash
cd client
npm run build:ios
```

2. **在 Xcode 中运行**：
```bash
cd client
npm run open:ios
```
然后点击运行按钮

3. **查看效果**：
   - 应用使用构建后的优化代码
   - 性能更好，但需要重新构建才能看到更改

## 📱 在 Xcode 中查看代码

### 查看 Web 代码（React/JavaScript）

1. **在 Cursor/VS Code 中编辑**：
   - 编辑 `client/src/` 中的文件
   - 使用实时预览模式查看效果

2. **在 Xcode 中查看**：
   - Xcode 主要用于查看和调试原生 iOS 代码
   - Web 代码主要在 `client/src/` 目录中

### 查看 iOS 原生代码

1. **打开 Xcode 项目**：
```bash
cd client
npm run open:ios
```

2. **查看文件结构**：
   - `App/AppDelegate.swift` - iOS 应用入口
   - `App/Info.plist` - 应用配置
   - `App/public/` - Web 资源（由 Capacitor 同步）

## 🔄 Git 工作流

### 在 Xcode 中使用 Git

1. **查看更改**：
   - `Source Control` → `Show Changes` (`⌘⇧⌘`)
   - 左侧导航栏显示修改的文件

2. **提交更改**：
   - `Source Control` → `Commit` (`⌘⌥C`)
   - 选择要提交的文件
   - 填写提交信息
   - 点击 `Commit`

3. **推送到 GitHub**：
   - `Source Control` → `Push` (`⌘⇧⌘K`)
   - 或使用终端：`git push`

### 在终端中使用 Git

```bash
# 查看状态
git status

# 添加更改
git add .

# 提交
git commit -m "描述更改"

# 推送
git push
```

## 🛠️ 常见问题

### Q: 模拟器中看不到最新更改？
A: 
1. 检查是否在实时预览模式（`npm run dev:ios`）
2. 确认前端开发服务器正在运行（`npm run dev`）
3. 检查 IP 地址是否正确（`capacitor.config.dev.ts`）

### Q: Xcode 中看不到 Git 选项？
A: 
1. 确保项目在 Git 仓库中
2. 在 Xcode 中：`File` → `Source Control` → `Create Git Repositories...`（如果还没初始化）

### Q: 如何切换回生产模式？
A: 
```bash
cd client
npm run prod:ios
```

### Q: IP 地址变化了怎么办？
A: 
1. 获取新 IP：`ifconfig | grep "inet " | grep -v 127.0.0.1`
2. 更新 `client/capacitor.config.dev.ts` 中的 `url`
3. 重新运行：`npm run dev:ios`

## 📝 快速命令参考

```bash
# 启动前端开发服务器
cd client && npm run dev

# 配置开发模式并同步
cd client && npm run dev:ios

# 在 Xcode 中打开
cd client && npm run open:ios

# 构建生产版本
cd client && npm run build:ios

# 查看 Git 状态
git status

# 提交并推送
git add . && git commit -m "描述" && git push
```

